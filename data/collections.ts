import type { DiscoveryCollection } from '@/types/school';

// Collections based on climate, mobility, retail, or airport access are not
// published until every included school has source-backed inputs.
export const collections: DiscoveryCollection[] = [];
